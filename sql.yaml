---
- name: Prepare basic environment for database replication between two servers.
  hosts: dbs
  remote_user: vagrant
  become: yes
  become_method: sudo
  roles:
    - tools
    - nfs
    - mariadb
